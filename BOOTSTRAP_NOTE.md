# Bootstrap in progress

This repository is being populated automatically by the one-time `Bootstrap Career Capital Intelligence` workflow. The workflow unpacks the tested zero-cost project, runs its tests, rebuilds the static site, commits the real project tree, and removes the temporary bootstrap archive/workflow.
